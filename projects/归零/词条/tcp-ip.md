# TCP/IP

互联网的基础协议栈。四层模型：

- 应用层：HTTP、SSH、DNS——应用程序直接使用的协议
- 传输层：TCP（可靠、有序）和 UDP（快速、不可靠）
- 网络层：IP——负责寻址和路由
- 链路层：以太网、Wi-Fi——物理传输

TCP 三次握手：SYN → SYN-ACK → ACK。连接建立后才能传输数据。

在《归零》中，wl0 不在 Linux 网络栈的任何一层里。`ip link` 认它——往下一层，什么都没有。林奕通过 wl0 发起的渗透不是普通的 TCP/IP 连接——是跨时空的代理隧道。
