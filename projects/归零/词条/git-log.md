# git log

终端系统中的观测命令。显示当前世界线上的事件快照——谁做了什么、什么时间、什么结果。只读。不能修改现实。

输出格式类似真实 git：
- 4 位 commit hash
- 时间戳
- commit message（自动或手动）

系统会自动记录林奕的操作和观测行为，标记为 `observed`。长时间无操作会标记为 `idle`。异常事件标记为 `anomaly`。

林奕后来发现 git log 中存在不属于他的 commit——作者是 `kiri`，分布在整个世界线树上，时间戳比他任何操作都早。

git log 是后视镜和地图——不是方向盘。
