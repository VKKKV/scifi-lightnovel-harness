# Base64 编码

将二进制数据转换为可打印 ASCII 字符的编码方式。每 3 字节原始数据 → 4 个 Base64 字符。字符集：A-Z、a-z、0-9、+、/（最后两个在某些变体中不同）。

不是加密——只是编码。任何人都可以解码。Base64 常被用于：
- 邮件附件（MIME）
- URL 参数中传递二进制数据
- JSON 中嵌入图片或文件
- 最基本的 CTF Crypto 入门题

在《归零》中，千夏做的第一道 CTF 密码学题就是 Base64 解码——花了三秒。第二道 ROT13——花了五秒。第三道 Vigenère——停了两分钟。
