# 端口（Network Port）

网络通信的逻辑端点。0-65535 范围。前 1024 个为「知名端口」：

- 22：SSH（远程 shell）
- 80：HTTP（网页）
- 443：HTTPS（加密网页）
- 3306：MySQL 数据库
- 5432：PostgreSQL 数据库
- 8080：HTTP 代理/开发服务器

扫描端口的常用工具：`nmap`。林奕在第一次渗透时用 nmap 扫描目标时间点的网络——发现端口扫描速度异常快，延迟几乎不存在。这是 wl0 隧道特性的第一个实证。
